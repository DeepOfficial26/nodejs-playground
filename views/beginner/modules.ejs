<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modules System - Node.js Interview Playground</title>
    <%- include('partials/styles') %>
</head>
<body>
    <div class="container">
        <%- include('partials/header') %>
        <%- include('partials/nav') %>
        
        <div class="card">
            <h2>CommonJS vs ES Modules</h2>
            <p>Understanding Node.js module systems.</p>
            
            <h3>CommonJS</h3>
            <div class="code-block">
                <pre><code><%= examples.commonjs.syntax %></code></pre>
            </div>
            <ul>
                <% examples.commonjs.characteristics.forEach(char => { %>
                    <li><%= char %></li>
                <% }); %>
            </ul>
            
            <h3>ES Modules</h3>
            <div class="code-block">
                <pre><code><%= examples.esModules.syntax %></code></pre>
            </div>
            <ul>
                <% examples.esModules.characteristics.forEach(char => { %>
                    <li><%= char %></li>
                <% }); %>
            </ul>
            
            <h3>When to Use</h3>
            <div class="topic-grid">
                <div class="topic-card">
                    <h3>CommonJS</h3>
                    <ul>
                        <% examples.whenToUse.commonjs.forEach(use => { %>
                            <li><%= use %></li>
                        <% }); %>
                    </ul>
                </div>
                <div class="topic-card">
                    <h3>ES Modules</h3>
                    <ul>
                        <% examples.whenToUse.esModules.forEach(use => { %>
                            <li><%= use %></li>
                        <% }); %>
                    </ul>
                </div>
            </div>
        </div>
        
        <%- include('partials/footer') %>
    </div>
</body>
</html>
