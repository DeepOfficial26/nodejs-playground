<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Event Loop Phases - Node.js Interview Playground</title>
    <%- include('../partials/styles') %>
</head>
<body>
    <div class="container">
        <%- include('../partials/header') %>
        <%- include('../partials/nav') %>
        
        <div class="card">
            <h2>Event Loop Phases</h2>
            <p>Deep dive into the six phases of the Node.js event loop.</p>
            
            <h3>Event Loop Phases</h3>
            <div class="topic-grid">
                <% demo.phases.forEach(phase => { %>
                    <div class="topic-card">
                        <h3><%= phase.name %></h3>
                        <p><%= phase.description %></p>
                    </div>
                <% }); %>
            </div>
            
            <div class="key-points">
                <h3>Key Points:</h3>
                <ul>
                    <% demo.keyPoints.forEach(point => { %>
                        <li><%= point %></li>
                    <% }); %>
                </ul>
            </div>
        </div>
        
        <%- include('../partials/footer') %>
    </div>
</body>
</html>
