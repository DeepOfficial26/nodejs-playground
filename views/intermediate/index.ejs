<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Intermediate Level - Node.js Interview Playground</title>
    <%- include('../partials/styles') %>
</head>
<body>
    <div class="container">
        <%- include('../partials/header') %>
        <%- include('../partials/nav') %>
        
        <div class="card">
            <h2>Intermediate Level - Async & Web</h2>
            <p>Master async patterns, streams, and middleware architecture.</p>
        </div>
        
        <div class="topic-grid">
            <% topics.forEach(topic => { %>
                <div class="topic-card">
                    <h3><%= topic.name %></h3>
                    <p><%= topic.description %></p>
                    <a href="<%= topic.path %>">Learn →</a>
                </div>
            <% }); %>
        </div>
        
        <%- include('../partials/footer') %>
    </div>
</body>
</html>
