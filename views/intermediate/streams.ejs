<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Streams & Buffers - Node.js Interview Playground</title>
    <%- include('partials/styles') %>
</head>
<body>
    <div class="container">
        <%- include('partials/header') %>
        <%- include('partials/nav') %>
        
        <div class="card">
            <h2>Streams & Buffers</h2>
            <p>Understanding streams for memory-efficient data processing.</p>
            
            <% examples.examples.forEach(example => { %>
                <h3><%= example.type %></h3>
                <div class="code-block">
                    <pre><code>Result: <%= example.result %></code></pre>
                </div>
                <p><%= example.explanation %></p>
            <% }); %>
            
            <div class="key-points">
                <h3>Key Points:</h3>
                <ul>
                    <% examples.keyPoints.forEach(point => { %>
                        <li><%= point %></li>
                    <% }); %>
                </ul>
            </div>
        </div>
        
        <%- include('partials/footer') %>
    </div>
</body>
</html>
